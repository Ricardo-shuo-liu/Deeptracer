### 统计于2025.12.9
>@Ricardo-shuo-liu

```
提交pr #15 #9 #8 #7 #6
主要实现了以下工作：
实现了coze的智能体接口以及文件传输的文本格式兼容问题
解决python2和python3在语法上的不兼容问题
deeeptracer基本环境的搭建

预测在下周中旬完成工作
```
---
>qscoder23
```

    1.在.github/workflows/目录下添加一个YAML文件，定义给予Github Action的CI workflow，实现对新PR代码风格的审核和优化，自动化测试

    2.添加了flake8和pre-commit的相关的配置，确保工作流的正常运行

```
---
>Amber-deal
```
做了两套接待方式：一套是命令行（CLI），在电脑上输个命令就能用；另一套是网页API，方便以后做个网站或者APP来调用。用的是 Click 和 FastAPI 这两个库命，令行有彩色提示，网页版还有自动生成的说明书（Swagger文档）。
搭建了“基础检查室”，实现了三个基础的代码检查功能：语法检查（用Python自带的 ast 库实现），代码度量（统计代码行数、函数数、类数、复杂度），结构分析（提取代码里的导入、函数、类这些结构）。
关于安全沙箱（CodeSandbox这里用了 subprocess 来隔离进程，不同操作系统（Windows/Linux）用了不同的安全策略。
用 Pydantic 库定义了一套标准的数据格式。不管是收到的请求，还是检查完返回的响应，都按固定格式来，前后端交接不会乱。
整了个“插件式工具箱”，把上面的检查功能都做成了可插拔的“工具”，方便后续功能的添加
```
---
>ha12300
```
第一周进展：
我用的是React 18作为前端框架，开发语言是TypeScript，然后就是用ECharts来制作图表。
这一周，在功能方面，大概可以分为三个模块吧，首先就是用Viztracer进行数据的处理，用来接收解析原始性能数据，通过数据清洗机制构建结构化的函数调用数据，简单来说就是数据的预处理吧。然后就是数据可视化这一模块，基于ECharts实现了条形图、折线图及执行流程图等可视化方面的展示，其实执行流程图就是根据tutor的代码分析页面来做的，目前的话就实现了执行控制，函数耗时和函数调用时间的可视化。最后交互式数据探索模块，支持多线程数据查看与分析，作用的话就是帮助开发者识别性能。
大体上，上周完成了测试支持体系的搭建工作。也是对项目技术栈进行了梳理，其次完成了Jest测试框架的安装与配置，包括安装和一些配置文件的创建，并且针对TypeScript特性及ES模块规范进行了针对性配置，对于数据处理模块的功能主要写了覆盖数据清洗、事件过滤、聚合功能、单线程数据处理及执行步骤生成等；

第一周基本上就做了这些，最主要就是数据清洗和可视化。
```
---
>Mantianxiaoyou
```
第一周进展：
我用采用vue做前端框架，第一周到现在正在进行基础框架的搭建，目前已经完成一部分基础复用vue组件编写，初步计划设置三个界面分别为1、呈现对程序运行开销的统计的分析界面，在界面中可以选择呈现哪些数据分析的工具。2、用diff2html库生成代码重构界面。3、智能体对话的窗口。目前整体框架还正处在搭建当中，目前已经完成初步的样式设计，状态管理与路由配置。
以上是第一周所做，主要都是在搭建框架
```